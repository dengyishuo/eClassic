% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_benchmark.R
\name{add_benchmark}
\alias{add_benchmark}
\title{Add Benchmark Returns to Data}
\usage{
add_benchmark(
  data,
  market_code,
  y_col = "ret_1",
  new_col = "benchmark_ret",
  keep_benchmark = FALSE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{data}{A data.frame or tibble in long format (required columns: \code{date},
\code{code}, and a return column for the benchmark). Typically obtained via
\code{get_data()} and then processed with \code{add_return()}.}

\item{market_code}{Character. The stock code of the benchmark index as it
appears in the \code{code} column (e.g., \code{"000300.SS"} for CSI300).}

\item{y_col}{Character. Name of the return column to use for the benchmark.
Default is \code{"ret_1"}.}

\item{new_col}{Character. Name of the new column to store the benchmark
returns. Default is \code{"benchmark_ret"}.}

\item{keep_benchmark}{Logical. If \code{TRUE}, keep rows where \code{code == market_code}
in the output; if \code{FALSE}, filter out those rows. Default is \code{FALSE} because
benchmark rows are usually not needed for factor analysis.}

\item{output}{Character. Either \code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
A data frame or tibble with an additional column containing
benchmark returns. If \code{keep_benchmark = FALSE}, benchmark rows are removed.
}
\description{
Extract returns of a benchmark index (e.g., CSI300) from the data and add
them as a new column to every row. This facilitates rolling beta calculations
against a market benchmark.
}
\details{
The function assumes that the input \code{data} already contains daily returns
(e.g., generated by \code{add_return()}) for both stocks and the benchmark index.
It extracts the benchmark return series by \code{date}, replicates it across all
stocks, and optionally discards the benchmark rows.
}
\examples{
\dontrun{
library(FactorCraft)
library(dplyr)

stock_df <- data.frame(
  code = c("000001.SZ", "000002.SZ", "000300.SS"),
  name = c("Ping An Bank", "Vanke A", "CSI300")
)

dat <- get_data(stock_df, start = "2024-01-01", end = "2024-12-31") |>
  add_return(n = 1, type = "discrete")

dat_with_bench <- add_benchmark(dat, market_code = "000300.SS")

head(dat_with_bench)
}
}
\seealso{
\code{\link{add_return}}, \code{\link{add_beta}}
}
