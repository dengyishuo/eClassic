% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_volatility.R
\name{add_volatility}
\alias{add_volatility}
\title{Add Rolling Volatility (Standard Deviation / Variance + Annualized)}
\usage{
add_volatility(
  data,
  close_col = "close",
  new_col = "vol",
  n = c(5, 10, 20),
  ret_type = c("continuous", "discrete"),
  vol_type = c("sd", "var"),
  annualized = FALSE,
  trade_days = 252,
  na.pad = TRUE,
  append = TRUE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{data}{Standard long data from get_data()}

\item{close_col}{Close price column}

\item{new_col}{Prefix for output columns}

\item{n}{Lookback periods (vector allowed: c(5,10,20))}

\item{ret_type}{Return type: "continuous" (log) or "discrete" (simple)}

\item{vol_type}{Volatility type: "sd" (standard deviation) or "var" (variance)}

\item{annualized}{Logical. If TRUE, annualize volatility (252 trading days)}

\item{trade_days}{Numeric. Annual trading days (default 252)}

\item{na.pad}{Pad leading NAs}

\item{append}{Append to data or return only factors}

\item{output}{"tibble" or "data.frame"}
}
\value{
Data frame with professional volatility factors
}
\description{
Professional rolling volatility calculator with:
\itemize{
\item Return type (log / simple)
\item Volatility type (std dev / variance)
\item Annualized volatility option
Automatically computes 1-day returns internally.
}
}
\examples{
\dontrun{
library(FactorCraft)
data(global_core_assets)

# Standard 20-day rolling std (not annualized)
dat1 <- add_volatility(global_core_assets, n = 20, vol_type = "sd")

# 20-day annualized volatility (institutional standard)
dat2 <- add_volatility(global_core_assets, n = 20, annualized = TRUE)

# Rolling variance
dat3 <- add_volatility(global_core_assets, vol_type = "var")
}
}
