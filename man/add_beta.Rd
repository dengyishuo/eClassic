% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_beta.R
\name{add_beta}
\alias{add_beta}
\title{Add Rolling Beta Factor}
\usage{
add_beta(
  data,
  y_col = "ret_1",
  x_col = "benchmark_ret",
  new_col = "beta",
  n = c(60, 120, 250),
  min_obs = 2L,
  append = TRUE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{data}{A data.frame or tibble in long format (required columns: \code{date},
\code{code}, \code{name}, and the variables used in regression). Typically obtained
via \code{get_data()} and \code{add_return()}, then enriched with benchmark returns
using \code{add_benchmark()}.}

\item{y_col}{Character. Name of the dependent variable column (e.g., \code{"ret_1"},
the stock's daily return). Default is \code{"ret_1"}.}

\item{x_col}{Character. Name of the independent variable column (e.g.,
\code{"benchmark_ret"}, the market index return). Default is \code{"benchmark_ret"}.}

\item{new_col}{Character. Prefix for the output beta columns. The actual
column names will be \code{paste0(new_col, "_", n)}. Default is \code{"beta"}.}

\item{n}{Integer vector. Rolling window lengths (lookback periods) in days.
Default is \code{c(60, 120, 250)} (approximately 3, 6, 12 months).}

\item{min_obs}{Integer. Minimum number of non-NA observation pairs required
to compute beta in a rolling window. Default is 2L.}

\item{append}{Logical. If \code{TRUE}, append the new beta columns to the
original data and return the full data set. If \code{FALSE}, return only
the columns \code{date}, \code{code}, \code{name}, and the beta columns.}

\item{output}{Character. Either \code{"tibble"} (default) or \code{"data.frame"}.
Defines the class of the returned object.}
}
\value{
A tibble or data.frame with added rolling beta columns named
\code{beta_n} (or custom prefix).
}
\description{
Calculate rolling beta (slope coefficient) from a linear regression of stock
returns on market returns (or any user-provided independent variable). Beta
measures the sensitivity of a stock's return to the market return, commonly
used in asset pricing and risk management.
}
\details{
The function computes rolling ordinary least squares regression of
\code{y_col ~ x_col} for each stock (grouped by \code{code}) after sorting by \code{date}.
The slope coefficient is the beta. The implementation uses \code{roll::roll_lm}
for efficiency.

Missing values are handled by \code{min_obs} in \code{roll_lm}: only windows with
at least \code{min_obs} non‑missing observation pairs produce a numeric beta;
otherwise \code{NA} is returned.
}
\examples{
\dontrun{
library(FactorCraft)
library(dplyr)

stock_df <- data.frame(
  code = c("000001.SZ", "000002.SZ", "000300.SS"),
  name = c("Ping An Bank", "Vanke A", "CSI300")
)

dat <- get_data(stock_df, start = "2024-01-01", end = "2024-12-31") |>
  add_return(n = 1, type = "discrete") |>
  add_benchmark(market_code = "000300.SS")

beta_data <- add_beta(dat, y_col = "ret_1", x_col = "benchmark_ret", n = 60)

head(beta_data)
}
}
\seealso{
\code{\link{add_return}}, \code{\link{add_benchmark}}, \code{\link{add_slope}}
}
